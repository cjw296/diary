[project]
name = "diary"
version = "0.1.0"
description = "A journal/diary to record things that have happened."
authors = ["Chris Withers <chris@withers.org>"]
readme = "README.rst"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "configurator>=3.2.0",
    "lark>=1.2.2",
    "pyyaml>=6.0.2",
    "requests>=2.32.3",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
    "pytest-cov>=5.0.0",
    "testfixtures>=8.3.0",
]
